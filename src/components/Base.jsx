import React, { Component } from 'react';
import '../styles/Base.scss';

class Base extends Component {
    render() {
        return (
            <div className="myClass">                
                Basic React Template with Webpack
            </div>

        );
    }
}

export default Base;